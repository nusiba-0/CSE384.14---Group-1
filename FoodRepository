package org.example.meowwoof.repository;


import org.example.meowwoof.model.FoodDistribution;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;
import java.util.List;


@Repository
public interface FoodRepository extends JpaRepository<FoodDistribution, Integer> {
   List<FoodDistribution> findByAreaId(Integer areaId);
}
